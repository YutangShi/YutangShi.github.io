---
import MainLayout from '../layouts/MainLayout.astro';
import { SITE_CONFIG } from '../utils/config';

// 模擬數據
const contactInfo = [
  {
    icon: '📧',
    title: '電子郵件',
    value: 'yutagshi@gmail.com',
    link: 'mailto:utagshi@gmail.com'
  },
  {
    icon: '💬',
    title: 'LineID',
    value: 'yutang-shi',
    link: 'https://line.me/ti/p/~yutang-shi'
  },
  {
    icon: '💼',
    title: 'LinkedIn',
    value: 'www.linkedin.com/in/yutang-shi-4109ab31/',
    link: 'https://www.linkedin.com/in/yutang-shi-4109ab31/'
  }
];

const faqs = [
  {
    question: '回覆時間通常需要多久？',
    answer: '我通常會在 24 小時內回覆您的訊息。如果是緊急事項，請直接撥打電話聯繫。'
  },
  {
    question: '可以安排視訊會議嗎？',
    answer: '當然可以！我很樂意透過 Google Meet、Zoom 或其他平台與您進行視訊討論。'
  },
  {
    question: '提供免費諮詢嗎？',
    answer: '是的，我提供 30 分鐘的免費初步諮詢，讓我們了解您的需求並討論可能的解決方案。'
  }
];
---

<MainLayout 
  title="聯絡我 - Allen Shi"
  description="與 Allen Shi 聯繫，討論您的專案需求或合作機會"
>
  <div class="container">
    <!-- Hero Section -->
    <section class="contact-hero">
      <div class="hero-content">
        <h1>聯絡我</h1>
        <p class="hero-description">
          有任何專案想法或合作機會嗎？我很樂意與您討論！
          請透過以下方式直接聯繫我。
        </p>
      </div>
    </section>

    <div class="contact-layout single-column">
      <!-- Contact Info -->
      <section class="contact-info-section">
        <div class="info-header">
          <h2>聯絡資訊</h2>
          <p>您可以透過以下方式直接聯繫我</p>
        </div>
        
        <div class="contact-info-grid">
          {contactInfo.map((info) => (
            <a href={info.link} class="contact-info-item">
              <div class="info-icon">{info.icon}</div>
              <div class="info-content">
                <h3>{info.title}</h3>
                <p>{info.value}</p>
              </div>
            </a>
          ))}
        </div>
      </section>
    </div>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="section-header">
        <h2>常見問題</h2>
        <p>關於聯絡和合作的常見疑問</p>
      </div>
      <div class="faq-grid">
        {faqs.map((faq) => (
          <div class="faq-item card">
            <h3>{faq.question}</h3>
            <p>{faq.answer}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="cta-content">
        <h2>準備開始您的專案了嗎？</h2>
        <p>不要猶豫，立即聯繫我討論您的想法！</p>
        <div class="cta-actions">
          <a href="mailto:allen@example.com" class="btn">立即發送郵件</a>
          <a href="/services" class="btn btn-secondary">查看服務項目</a>
        </div>
      </div>
    </section>
  </div>
</MainLayout>

<style>
  /* Hero Section */
  .contact-hero {
    text-align: center;
    padding: var(--spacing-3xl) 0;
    margin-bottom: var(--spacing-2xl);
  }
  
  .hero-content {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .hero-content h1 {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--spacing-md);
  }
  
  .hero-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
  }
  
  /* Contact Layout */
  .contact-layout {
    display: flex;
    gap: var(--spacing-2xl);
    margin-bottom: var(--spacing-3xl);
  }
  
  .single-column {
    flex-direction: column;
    max-width: 800px;
    margin: 0 auto;
  }
  
  .contact-info-section, 
  .contact-form-section {
    flex: 1;
  }
  
  /* Section Headers */
  .info-header,
  .form-header,
  .section-header {
    margin-bottom: var(--spacing-xl);
  }
  
  .info-header h2,
  .form-header h2,
  .section-header h2 {
    font-size: var(--font-size-2xl);
    margin-bottom: var(--spacing-xs);
  }
  
  .info-header p,
  .form-header p,
  .section-header p {
    color: var(--color-text-light);
  }
  
  /* Contact Info Styles */
  .contact-info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
  }
  
  .contact-info-item {
    display: flex;
    align-items: center;
    padding: var(--spacing-md);
    border-radius: var(--border-radius-lg);
    background-color: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    transition: all 0.3s ease;
    text-decoration: none;
    color: var(--color-text);
  }
  
  .contact-info-item:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-md);
    border-color: var(--color-primary);
  }
  
  .info-icon {
    font-size: 1.5rem;
    margin-right: var(--spacing-md);
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(var(--color-primary-rgb), 0.1);
    border-radius: 50%;
  }
  
  .info-content h3 {
    font-size: var(--font-size-base);
    margin-bottom: 0.25rem;
    font-weight: 600;
  }
  
  .info-content p {
    font-size: var(--font-size-sm);
    color: var(--color-text-light);
    margin-bottom: 0;
  }

  /* FAQ Section */
  .faq-section {
    margin-bottom: var(--spacing-3xl);
    padding-bottom: var(--spacing-3xl);
  }
  
  .faq-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
  }
  
  .faq-item h3 {
    margin-bottom: var(--spacing-md);
    color: var(--color-primary);
  }
  
  .faq-item p {
    color: var(--color-text-light);
    line-height: 1.6;
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    padding: var(--spacing-3xl);
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    border-radius: var(--border-radius-lg);
    color: white;
  }
  
  .cta-content h2 {
    margin-bottom: var(--spacing-md);
    color: white;
  }
  
  .cta-content p {
    margin-bottom: var(--spacing-xl);
    opacity: 0.9;
  }
  
  .cta-actions {
    display: flex;
    justify-content: center;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }
  
  .cta-actions .btn {
    background-color: white;
    color: var(--color-primary);
  }
  
  .cta-actions .btn:hover {
    background-color: var(--color-bg-secondary);
  }
  
  .cta-actions .btn-secondary {
    background-color: transparent;
    color: white;
    border: 2px solid white;
  }
  
  .cta-actions .btn-secondary:hover {
    background-color: white;
    color: var(--color-primary);
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .contact-layout {
      grid-template-columns: 1fr;
      gap: var(--spacing-3xl);
    }
  }
  
  @media (max-width: 768px) {
    .form-row {
      grid-template-columns: 1fr;
    }
    
    .contact-form {
      padding: var(--spacing-lg);
    }
    
    .faq-grid {
      grid-template-columns: 1fr;
    }
    
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>

<script>
  // 表單提交處理
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('.contact-form');
    
    if (form) {
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // 這裡可以添加表單驗證和提交邏輯
        alert('感謝您的訊息！我會盡快回覆您。');
        
        // 重置表單
        form.reset();
      });
    }
  });
</script> 