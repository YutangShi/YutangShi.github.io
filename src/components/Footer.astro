<footer class="site-footer">
  <div class="container footer-contact" aria-labelledby="footer-contact-title">
    <h2 id="footer-contact-title">聯絡資訊</h2>
    <dl>
      <div>
        <dt>電子郵件</dt>
        <dd><a href="mailto:yutangshi@gmail.com">yutangshi@gmail.com</a></dd>
      </div>
      <div>
        <dt>LINE ID</dt>
        <dd>yutang-shi</dd>
      </div>
    </dl>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 4rem;
    padding: 2.2rem 0;
    border-top: 1px solid var(--color-border);
    background: #f8fbff;
  }

  .footer-contact {
    display: grid;
    grid-template-columns: auto 1fr;
    align-items: center;
    gap: 2rem;
    padding: 1rem;
    border: 1px solid var(--color-border);
    border-radius: 0.95rem;
    background: #ffffff;
  }

  .footer-contact h2 {
    margin: 0;
    font-size: 1rem;
    color: var(--color-text);
  }

  .footer-contact dl {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 0.75rem 2rem;
    margin: 0;
  }

  .footer-contact dl > div {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
  }

  .footer-contact dt {
    color: var(--color-text-light);
    font-size: 0.88rem;
  }

  .footer-contact dd {
    margin: 0;
    font-weight: 600;
  }

  .footer-contact a {
    color: var(--color-primary-dark);
  }

  .footer-contact a:hover {
    text-decoration-thickness: 2px;
  }

  .footer-contact a:focus-visible {
    outline: 2px solid var(--color-primary-dark);
    outline-offset: 3px;
  }

  @media (max-width: 640px) {
    .footer-contact {
      grid-template-columns: 1fr;
      gap: 0.85rem;
    }

    .footer-contact dl {
      display: grid;
      justify-content: start;
      gap: 0.65rem;
    }
  }
</style>
